language: node_js
node_js: "node"
before_install:
  - npm i -g yarn
install:
  - yarn install --frozen-lockfile --no-cache
script:
  - yarn test
  - yarn build
deploy:
  provider: script
  script: bash scripts/deploy.sh
